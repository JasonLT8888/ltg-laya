export class LayerDefine {

    static Player = Math.pow(2, 0);

    static Floor = Math.pow(2, 1);

    static Item = Math.pow(2, 2);

    static Ray = Math.pow(2, 3);

    static Wall = Math.pow(2, 4);

}